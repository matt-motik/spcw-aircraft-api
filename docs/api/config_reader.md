# Модуль: `config_reader.py`

*Сгенерировано: 2026-07-27 23:14:50*

---

<div id="ConfigError"></div>

## ConfigError

**Тип:** class

**Кратко:** Исключение при ошибках чтения конфигурации.

### Полная документация

```python
Исключение при ошибках чтения конфигурации.
```

---

<div id="get_db_config"></div>

## get_db_config

**Тип:** function

**Кратко:** Читает параметры подключения к БД из INI-файла.

### Полная документация

```python
Читает параметры подключения к БД из INI-файла.

Args:
    filename: Имя конфигурационного файла (относительно корня проекта).
    section: Название секции с параметрами БД.

Returns:
    Словарь с параметрами подключения (все значения — строки).

Raises:
    ConfigError: Если файл не найден или секция отсутствует.

Example:
    >>> config = get_db_config()
    >>> print(config["host"])
    localhost
```

---

